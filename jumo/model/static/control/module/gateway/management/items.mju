// Module: GatewayManagement — 网关自身管理域
//
// WistGateway 控制平台自身的领域事实：网关实例、控制面配置与健康状态。
// 区别于 insightcenter 层 GatewaySupervision 从中心侧对网关的接入与聚合。

struct WistGatewayInstance {
  meta {
    label zh "WistGateway 实例"
    label en "WistGateway Instance"
    tags "gateway", "instance"
  }

  unique gateway_id: String
  instance_id: String
  boot_id: String
  version: String
  started_at: DateTime
  last_seen_at: DateTime
}

struct GatewayControlConfig {
  meta {
    label zh "网关控制面配置"
    label en "Gateway Control Config"
    tags "gateway", "config"
  }

  unique config_id: String
  gateway_id: String
  advertise_url: String
  enrollment_url: String
  gateway_url: String
  telemetry_url: String
  updated_at: DateTime
}

struct GatewayHealth {
  meta {
    label zh "网关健康状态"
    label en "Gateway Health"
    tags "gateway", "health"
  }

  gateway_id: String
  status: String
  reported_at: DateTime
}

struct GatewayManagementState {
  meta {
    label zh "网关管理状态视图"
    label en "Gateway Management State View"
    tags "gateway", "management", "self-management"
  }

  gateway_id: String
  instance_id: String
  version: String
  status: String
  config: GatewayControlConfig
  health: GatewayHealth
  last_seen_at: DateTime
}


// 管理面看当前生效的发现方向策略表。
//
// 为什么视图在 Control 域而不是 Discovery：它是**网关管理面的读模型**，
// 与 GatewayManagementState 同类；但它的内容是 Discovery 域的对象（跨域引用）。
// 管理面看当前生效的策略表。
struct DiscoveryPolicyView {
  meta {
    label zh "发现方向策略视图"
    label en "Discovery Policy View"
    summary zh "管理面看当前生效的发现方向策略表：是否已配置、生效的那一版内容，以及每台 Agent 实际生效的版本。"
    tags "discovery", "policy", "view", "admin"
  }

  // 为什么带 `configured`：网关**没配**表时就不提供下发端点（agent 回退内建默认值），
  // 运维必须能区分「没配」与「配了一版空表」—— 后者不是合法状态（装载校验会拒）。
  configured: Boolean
  // 未配置时为空。
  policy: DiscoveryAspectPolicySet?
  // 每台已注册 Agent 实际生效的版本：回答「我改了策略，哪些机器还没生效」。
  agents: List<AgentAppliedDiscoveryPolicy>
  generated_at: DateTime
}

// 一台 Agent 的策略生效情况（管理面视图的一行）。
struct AgentAppliedDiscoveryPolicy {
  meta {
    label zh "Agent 策略生效情况"
    label en "Agent Applied Discovery Policy"
    summary zh "一台 Agent 实际生效的发现方向策略版本与最近一次状态上报时间。"
    tags "discovery", "policy", "view", "admin"
  }

  agent_id: String
  // 空 = 这台机器还没拉到策略表（在用内建默认周期）。**0 是一个真实版本，不可混同**。
  applied_policy_version: Int?
  instance_id: String
  // 空 = 从未上报过状态（只有注册记录）。与“上报过”区分开：没上报过的机器
  // 正是最可能漏掉的那一批，不能从列表里静默消失，也不能拿注册时刻冒充“最后一次上报”。
  last_seen_at: DateTime?
}

command ViewGatewayManagementState = OperatorRequest {
  meta {
    label zh "查看网关管理状态"
    label en "View Gateway Management State"
    tags "gateway", "management"
  }

}
