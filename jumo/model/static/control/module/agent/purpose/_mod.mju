// Control.Agent.Purpose 有界上下文：这台机器是干什么用的（用途识别）。
//
// 识别分两种，**处理路径完全不同**，不要混成一个字段：
//   确定 —— 操作系统、架构、计算资源（核数/内存/磁盘/GPU）：agentd 直接读到就报，无需判断；
//   非确定 —— 用途：动态、会变。由 agentd 采集事实（进程/监听端口/容器…），
//             **在网关侧**按规则表推断出“建议 + 命中依据 + 置信度”，再由人工判定归档。
//
// 三分：事实（agentd 报）→ 推断（网关算，可变可过期）→ 判定（人定，留痕）。
// 冲突时**以判定为准，但并列展示推断** —— 不是谁盖掉谁。
//
// 为什么不放 agentd：agentd 只报事实；规则表是“策展知识”，与采集目录同类，
// 由网关集中管理与更新；进程路径/参数里可能带用户名与敏感参数，不该为分类而外送。
//
// 规则表是数据（jumo/model/content/purpose-rules.toml），本模块只定义形状。

module<entity> Agent.Purpose {
  meta {
    label zh "Agent 用途识别"
    label en "Agent Purpose"
    summary zh "按事实在网关侧推断 Agent 的机器类别（建议 + 命中依据 + 置信度），再由人工判定归档：推断可过期，判定留痕。"
    tags "control", "agentd", "purpose"
  }

  layer Domain
}
